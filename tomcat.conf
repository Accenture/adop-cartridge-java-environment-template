server{
  listen 80;
  server_name ~^###TOMCAT_SERVICE_NAME###*;

  access_log  /var/log/nginx/access.log logstash;

  auth_ldap "Forbidden";
  auth_ldap_servers adop;

  proxy_set_header host $host;

  location /petclinic {
     proxy_pass  http://###TOMCAT_SERVICE_NAME###:8080/petclinic;
  }
}